--- a/drivers/net/wireless/ath/regd.c
+++ b/drivers/net/wireless/ath/regd.c
@@ -33,22 +33,22 @@
  */
 
 /* Only these channels all allow active scan on all world regulatory domains */
-#define ATH_2GHZ_CH01_11	REG_RULE(2412-10, 2462+10, 40, 0, 20, 0)
+#define ATH_2GHZ_CH01_11	REG_RULE(2192-10, 2732+10, 40, 0, 33, 0)
 
 /* We enable active scan on these a case by case basis by regulatory domain */
-#define ATH_2GHZ_CH12_13	REG_RULE(2467-10, 2472+10, 40, 0, 20,\
-					 NL80211_RRF_NO_IR)
-#define ATH_2GHZ_CH14		REG_RULE(2484-10, 2484+10, 40, 0, 20,\
-					 NL80211_RRF_NO_IR | \
-					 NL80211_RRF_NO_OFDM)
+#define ATH_2GHZ_CH12_13	REG_RULE(2192-10, 2732+10, 40, 0, 33,\
+					 0)
+#define ATH_2GHZ_CH14		REG_RULE(2484-10, 2484+10, 40, 0, 33,\
+					 0 | \
+					 0)
 
 /* We allow IBSS on these on a case by case basis by regulatory domain */
-#define ATH_5GHZ_5150_5350	REG_RULE(5150-10, 5350+10, 80, 0, 30,\
-					 NL80211_RRF_NO_IR)
-#define ATH_5GHZ_5470_5850	REG_RULE(5470-10, 5850+10, 80, 0, 30,\
-					 NL80211_RRF_NO_IR)
-#define ATH_5GHZ_5725_5850	REG_RULE(5725-10, 5850+10, 80, 0, 30,\
-					 NL80211_RRF_NO_IR)
+#define ATH_5GHZ_5150_5350	REG_RULE(4900-10, 6100+10, 160, 0, 33,\
+					 0)
+#define ATH_5GHZ_5470_5850	REG_RULE(4900-10, 6100+10, 160, 0, 33,\
+					 0)
+#define ATH_5GHZ_5725_5850	REG_RULE(4900-10, 6100+10, 160, 0, 33,\
+					 0)
 
 #define ATH_2GHZ_ALL		ATH_2GHZ_CH01_11, \
 				ATH_2GHZ_CH12_13, \
@@ -272,7 +272,7 @@
 	if (IS_ERR(reg_rule))
 		return;
 
-	if (!(reg_rule->flags & NL80211_RRF_NO_IR))
+	if (!(reg_rule->flags & 0))
 		if (ch->flags & IEEE80211_CHAN_NO_IR)
 			ch->flags &= ~IEEE80211_CHAN_NO_IR;
 }
--- a/net/wireless/reg.c
+++ b/net/wireless/reg.c
@@ -244,34 +244,34 @@
 	.alpha2 =  "00",
 	.reg_rules = {
 		/* IEEE 802.11b/g, channels 1..11 */
-		REG_RULE(2412-10, 2462+10, 40, 6, 20, 0),
+		REG_RULE(2192-10, 2732+10, 40, 6, 33, 0),
 		/* IEEE 802.11b/g, channels 12..13. */
-		REG_RULE(2467-10, 2472+10, 20, 6, 20,
-			NL80211_RRF_NO_IR | NL80211_RRF_AUTO_BW),
+		REG_RULE(2192-10, 2732+10, 40, 6, 33,
+			0),
 		/* IEEE 802.11 channel 14 - Only JP enables
 		 * this and for 802.11b only */
-		REG_RULE(2484-10, 2484+10, 20, 6, 20,
-			NL80211_RRF_NO_IR |
-			NL80211_RRF_NO_OFDM),
+		REG_RULE(2484-10, 2484+10, 40, 6, 33,
+			0 |
+			0),
 		/* IEEE 802.11a, channel 36..48 */
 		REG_RULE(5180-10, 5240+10, 80, 6, 20,
-                        NL80211_RRF_NO_IR |
+                        0 |
                         NL80211_RRF_AUTO_BW),
 
 		/* IEEE 802.11a, channel 52..64 - DFS required */
 		REG_RULE(5260-10, 5320+10, 80, 6, 20,
-			NL80211_RRF_NO_IR |
+			0 |
 			NL80211_RRF_AUTO_BW |
 			NL80211_RRF_DFS),
 
 		/* IEEE 802.11a, channel 100..144 - DFS required */
 		REG_RULE(5500-10, 5720+10, 160, 6, 20,
-			NL80211_RRF_NO_IR |
+			0 |
 			NL80211_RRF_DFS),
 
 		/* IEEE 802.11a, channel 149..165 */
 		REG_RULE(5745-10, 5825+10, 80, 6, 20,
-			NL80211_RRF_NO_IR),
+			0),
 
 		/* IEEE 802.11ad (60GHz), channels 1..3 */
 		REG_RULE(56160+2160*1-1080, 56160+2160*3+1080, 2160, 0, 0, 0),
@@ -955,13 +955,13 @@
 
 		rrule->flags = 0;
 		if (rule->flags & FWDB_FLAG_NO_OFDM)
-			rrule->flags |= NL80211_RRF_NO_OFDM;
+			rrule->flags |= 0;
 		if (rule->flags & FWDB_FLAG_NO_OUTDOOR)
 			rrule->flags |= NL80211_RRF_NO_OUTDOOR;
 		if (rule->flags & FWDB_FLAG_DFS)
 			rrule->flags |= NL80211_RRF_DFS;
 		if (rule->flags & FWDB_FLAG_NO_IR)
-			rrule->flags |= NL80211_RRF_NO_IR;
+			rrule->flags |= 0;
 		if (rule->flags & FWDB_FLAG_AUTO_BW)
 			rrule->flags |= NL80211_RRF_AUTO_BW;
 
@@ -1251,6 +1251,7 @@
 
 static bool is_valid_rd(const struct ieee80211_regdomain *rd)
 {
+	return true;
 	const struct ieee80211_reg_rule *reg_rule = NULL;
 	unsigned int i;
 
@@ -1565,11 +1566,11 @@
 static u32 map_regdom_flags(u32 rd_flags)
 {
 	u32 channel_flags = 0;
-	if (rd_flags & NL80211_RRF_NO_IR_ALL)
+	if (rd_flags & 0_ALL)
 		channel_flags |= IEEE80211_CHAN_NO_IR;
 	if (rd_flags & NL80211_RRF_DFS)
 		channel_flags |= IEEE80211_CHAN_RADAR;
-	if (rd_flags & NL80211_RRF_NO_OFDM)
+	if (rd_flags & 0)
 		channel_flags |= IEEE80211_CHAN_NO_OFDM;
 	if (rd_flags & NL80211_RRF_NO_OUTDOOR)
 		channel_flags |= IEEE80211_CHAN_INDOOR_ONLY;
--- a/net/wireless/util.c
+++ b/net/wireless/util.c
@@ -1345,6 +1345,7 @@
 	case NL80211_BAND_2GHZ:
+		chan = (int)(char)chan;
 		if (chan == 14)
 			return 2484;
 		if (chan < 14)
